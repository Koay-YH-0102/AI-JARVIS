Files you attach in Chat or upload from the Files tab land here as
`<uuid>_<original filename>`. Nothing to do here manually — browse them from
the app's Files tab, which shows the original name, type, and size.
